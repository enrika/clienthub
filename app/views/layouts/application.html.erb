<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Clienthub</title>
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta content="width=device-width, initial-scale=1" name="viewport" />
        <meta content="" name="storysitez client projects" />
        <meta content="" name="Enrika Greathouse" />
          <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
          <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
          <%= csrf_meta_tags %>
</head>


<link rel="shortcut icon" href="favicon.ico" /> </head>
    <!-- END HEAD -->

    <body class="page-container-bg-solid page-boxed">
    <div class="big_container">
          <div class="topbar">
              <div class="logospace">
                <%= link_to(image_tag('szlogo.png'), opportunities_path, class:  "logo") %>
              </div>
              <div class="signinspace">
                  <% if current_user %>
                   <%= link_to("Hi " + current_user.name.split.first, current_user, class: 'signin') %>
                   <%= link_to("Sign Out", session_path, method: :delete, class: 'button signin' ) %>
                  <% else %>
                  <%= link_to('Sign In', new_session_path, :class => "signin") %>
                  <% end %>
              </div>
          </div>
          <div class="menubar">
                
                <%=   add_new_button  %> 
                 
          </div>
                <div class="wrapper">
                  <%= render 'layouts/flashes' %>
   
<!-- main container with all dynamic content -->
                <%= yield %>
            
    <%= render 'layouts/footer' %>
    </div>
    </div>

</body>
</html>
